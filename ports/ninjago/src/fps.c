/* fps.c -- on-screen FPS counter stub (Linux port)
 *
 * The Switch build drew a bitmap-font FPS counter; not needed for bring-up.
 * config.show_fps defaults to 0, so this is a no-op unless re-enabled.
 */

#include "fps.h"

void fps_render(void) {}
