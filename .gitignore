# build artifacts
build/
ports/*/build/
*.o
*.a
*.elf
*.bin

# logs
log.txt
*.log

# === BYO game files — NUNCA commitar conteudo de jogo (copyright) ===
*.so
*.obb
*.apk
*.dat
*.unity3d
*.resource
*.assets
sharedassets*
data.unity3d
global-metadata.dat

# OS
.DS_Store
experiments/*/build/

# dados/libs/binarios do jogo (BYO-data estilo PortMaster -- NUNCA no repo)
ports/*/assets/
ports/*/*.zip
ports/*/*.idx
ports/*/*.obb
ports/*/*.so
ports/*/*.swap
ports/bully/bully
ports/bfbc2/bfbc2
mem.log
bully.log

# dados/libs locais GRANDES (>100MB, NUNCA stagear)
experiments/*/gamefiles/
experiments/*/gamefiles-arm64/
experiments/*-pc/gamefiles/

# referencias de terceiros (nao distribuir)
experiments/bully/ref-*/

# binarios compilados (build artifacts -- gerados por build.sh)
ports/gtavc/gtavc
ports/revc/revc
experiments/hollow-recon/hollow-recon-no
experiments/hollow-recon/evdev2key

# build output .NET (gerado por build/publish -- NAO versionar)
ports/sor4/port/tools/texconv/bin/
ports/sor4/port/tools/texconv/obj/
ports/sor4/port/tools/texconv/out/
__pycache__/
*.pyc
# dumps de gdb (debug efemero)
experiments/hollow-recon/*.mem
# crazytaxi: SO o codigo/loader vai pro repo. Dados de jogo (copyright Sega) e
# binarios compilados NUNCA -- PROIBIDO subir dados de jogo pro GitHub.
ports/crazytaxi/package/
ports/crazytaxi/assets/
ports/crazytaxi/crazytaxi
ports/crazytaxi/*.so
ports/graveyardkeeper/graveyardkeeper
# build artifacts das ferramentas .NET (Cecil) -- reproduziveis de *.cs, NAO versionar
ports/sor4/port/tools/*/bin/
ports/sor4/port/tools/*/obj/
*.pdb

# game payloads (conteudo de jogo extraido — nunca commitar)
ports/*/payload/
*.orig

# shantae: binario + payload (lib do jogo, copyright)
ports/shantae/shantae
ports/shantae/payload/

# game data / large artifacts (never commit - reproduce from APK/source)
ports/*/deploy/gamedata/
**/*.lgp
**/*.webm
**/*.obb
ports/ff9/userdata/
ports/ff9/bin/Data/

# dados de jogo extras (audio stream / game.droid / binarios de port)
*.ogg
*.droid
ports/*/katanazero
ports/*/*.zero
options.ini
ports/sonic4/device_libs_3.0.0_arm64/

# === NextOS: nunca commitar (runs/logs/backups de debug) ===
ports/*/runs/
ports/*/logs/
*.bak
*.OURS-BACKUP
*.PRE-DBG
*.raw

# === REFORCO 2026-07-06: blindagem contra dado de jogo em ports novos ===
# arquivos-dump / arquivos grandes de device
*.tar
*.tar.gz
*.tgz
ports/*-device.tar
# bancos de dados / fontes / dados de app (copyright do jogo)
*.db
*.db-journal
*.sqlite
*.ttf
*.otf
*.mmdb
*.plist
*.mp3
*.wav
*.ktex
*.zip
# diretorios que so tem binario/dado (ferramentas compiladas, backups, checkpoints, app data)
ports/*/tools/
ports/*/backups/
ports/*/checkpoints/
ports/*/databases/
ports/*/files/
ports/*/lib/
ports/*/obb/
ports/*/bin/
ports/*/game/
ports/*/payload/
ports/*/userdata/
# loaders compilados dos ports novos (ELF sem extensao — reproduzir do build.sh)
ports/badland/badland
ports/dontstarve/dontstarve
ports/legendofmana/legendofmana
ports/limbo/limbo
ports/magicrampage/magicrampage
ports/rockmanxdive/rockmanxdive
ports/sonic4/sonic4.arm64
ports/pes2012/pes2012
ports/cvgos/cvgos
ports/tasm2/tasm2
# bytecode / decompilacao / prefs de app (game data + tokens/privacidade)
*.dex
*.rzdb
ports/*/inspect/
ports/*/shared_prefs/
ports/*/dex_decrypted/
ports/*/dex_dump/
ports/*/smali/
AndroidManifest.xml
# dumps de engenharia reversa (disassembly/xref do binario do jogo) + app data residual
ports/*/rz_*.txt
ports/*/datacenter
ports/*/*.rz.txt
ports/elderand/elderand
ports/kof2012a/kof2012a
ports/legobatman2/legobatman2
ports/legobatman2/files/
